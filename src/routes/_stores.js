import { writable } from 'svelte/store'

export const region = writable(null)
export const accessKeyId = writable(null)
export const secretAccessKey = writable(null)

export const client = writable(null)
